import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./應用程式";

const root = ReactDOM.createRoot(document.getElementById("根"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
